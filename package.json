{
  "name": "smart-connections-lmstudio-embeddings",
  "version": "0.1.16",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "obsidian": "latest",
    "typescript": "^5.6.3"
  }
}
